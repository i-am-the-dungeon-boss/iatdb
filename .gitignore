# Built application files
*.apk

# Files for the Dalvik VM
*.dex

# Java class files
*.class

# Gradle build files
.gradle/
build/
/release/
/dist/

# Local configuration file (sdk path, etc)
local.properties

#intellij project files
*.iml
*.idea

# Metals (Scala/Java language server)
.metals/
.bloop/
.bsp/

# Eclipse / Java language server (auto-generated; use .vscode/java.prefs instead)
.classpath
.project
.factorypath
.settings/

# Runtime game data (generated locally)
desktop/leaderboard.json
leaderboard-solo.json
echoes-solo/

# Local secrets (copy from .env.example)
.env

# Sentry auth / CLI (never commit tokens). Desktop DSN file under
# desktop/src/main/resources/sentry.properties is intentionally committed.
/sentry.properties
android/sentry.properties
.sentryclirc
.env.sentry-build-plugin

# Android release signing (never commit; see android/keystore.properties.example)
android/keystore.properties
android/*.keystore
android/*.jks
android/*.jks.old
