# Copy to keystore.properties (gitignored) next to this file.
# Used by android/build.gradle to sign release APK/AAB builds.
#
# storeFile is relative to the android/ module directory.

storeFile=iatdb-release.keystore
storePassword=REPLACE_ME
keyAlias=iatdb
keyPassword=REPLACE_ME
